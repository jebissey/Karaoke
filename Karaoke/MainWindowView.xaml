<Window x:Class="Karaoke.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Karaoke"
        Title="MainWindow" Height="400" Width="800">
    
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Text="{Binding Lyrics}" Height="300" Width="750" FontSize="36" TextAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="⏵" Width="74"  Command="{Binding StartCommand}"/>
            <Button Content="⏸" Width="74" Command="{Binding PauseCommand}"/>
            <Button Content="⏹" Width="74" Command="{Binding StopCommand}"/>
        </StackPanel>
        
        <TextBlock Grid.Row="3" HorizontalAlignment="Left" Text="{Binding CurrentTime}" />
        <TextBlock Grid.Row="3" HorizontalAlignment="Right" Text="{Binding TotalTime}" />
        <Slider Grid.Row="4" HorizontalAlignment="Center" Width="750" Maximum="{Binding SliderMax}" Value="{Binding SliderValue, Mode=TwoWay}"/>
    </Grid>
</Window>
